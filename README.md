## Local Setup

1. Install dependencies:

   npm install

2. Run development server:

   npm run dev

3. Run lint checks:

   npm run lint

4. Run formatter:

   npm run format

All changes must go through PR with passing CI checks.
