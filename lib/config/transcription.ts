/** Maximum character count of final-entry text before old entries are pruned. */
export const TRANSCRIPT_WINDOW_CHAR_LIMIT = 50_000;

/** Maximum number of turns kept in the frontend turn buffer. */
export const FRONTEND_TURN_LIMIT = 200;
